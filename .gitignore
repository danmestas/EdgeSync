# Fossil reference checkouts (local only, read-only reference for porting)
fossil/
libfossil/
.fossil-settings/

# Fossil SQLite databases (binary, should never be in git)
*.fossil
.fslckout

# Build artifacts
/bin/
/soak
/edgesync
/leaf/leaf
.worktrees/
iroh-sidecar/target/

# Merge test artifacts
*.LOCAL
*.REMOTE
*.BASE
*.BASELINE
*.MERGE
f.txt

# Secrets / environment
.env
.env.*

# Agent/skill tool files
.agents/
AGENTS.md
skills-lock.json
.DS_Store
.gitnexus
.claude/worktrees/
.claude/skills/
go.work
go.work.sum

# Temporary clones for development
.tmp-libfossil/

# Hugo / docs site build artifacts (regenerated on Cloudflare deploy)
docs/site/public/
docs/site/resources/
docs/site/.hugo_build.lock
docs/site/static/llms.txt
docs/site/static/llms-full.txt
# Leftover Hugo binary downloaded by build-docs.sh when system hugo isn't on PATH
/hugo

# Wrangler / Node
node_modules/
.wrangler/

# Local-only MCP config (personal paths, internal endpoints)
.mcp.json
